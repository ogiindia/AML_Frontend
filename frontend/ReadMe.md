`pnpm config set auto-install-peers true` to install peerdependencies
`pnpm clean:install` to clean node modules and re-install
`pnpm add dependency` to add dependencies to root which is shareable by all or for specific child project
`pnpm add localpluginname --workspace` to install from local and not look in npm repo.
